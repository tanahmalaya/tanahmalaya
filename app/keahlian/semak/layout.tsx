export const metadata = {
  title: "Semak Status Keahlian Ahli Bersekutu",
  robots: { index: false, follow: false },
};

export default function SemakLayout({ children }: { children: React.ReactNode }) {
  return children;
}
