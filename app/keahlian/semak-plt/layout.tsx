export const metadata = {
  title: "Semak Status Keahlian Ahli PLT",
  robots: { index: false, follow: false },
};

export default function SemakPltLayout({ children }: { children: React.ReactNode }) {
  return children;
}
